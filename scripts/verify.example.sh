#!/usr/bin/env bash
# Example only — copy to an application repo as scripts/verify.sh and replace
# with real lint/test/build commands. chmod +x scripts/verify.sh

set -euo pipefail

echo "Replace this script with your project's lint, test, and build steps."
exit 1
