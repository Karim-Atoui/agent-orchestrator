# Orchestrate

**Canonical workflow:** read the **orchestrator** skill’s `SKILL.md`, then `reference.md` for thresholds and policies. Resolve the skill from either location Cursor merges:

- **User (default):** `~/.cursor/skills/orchestrator/`
- **Project override:** `.cursor/skills/orchestrator/` in the repo

**Do not duplicate** steps or numbers here—the skill is the single source of truth.

---

## Task (user — keep or replace)

**Goal:**

**GitHub issue (optional):**

**Constraints (optional):**

---

## Instruction

Run the **orchestrator** skill **end-to-end** for this workspace until the pipeline completes or you hit a mandatory stop (plan gate, escalation, or user interrupt).
